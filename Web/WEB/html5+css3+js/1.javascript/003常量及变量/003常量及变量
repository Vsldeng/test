数据类型分为两类（变量）
  基本数据类型
    String Number boolean null（空） undefined（未声明）
  复合数据类型

变量的声明：
  var age=18
  定义变量的时候，同时给变量赋值，叫做初始化
  同时定义多个变量，要用,隔开

标识符：用户自定义的所有名字叫做标识符
规律：
1.标识符必须由数字、字母、下划线和美元符号$组成
2.不能以数字开头
3.标识符区分大小写
4.标识符最好具有语义化

typeof x//判断x的数据类型
  js是弱语言，他被赋值什么类型，就是什么类型
  最好不要在赋值后，再修改变量的类型
